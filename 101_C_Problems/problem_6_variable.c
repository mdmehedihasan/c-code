/*take three integer variable a,b and c. Assign values to the variables a=10,b=20 and c=30, Now print the sum of these three variables*/

#include <stdio.h>
int main()
{
    int a = 10, b = 20, c = 30;
    printf("Sum of value is: %d", a + b + c);
    return 0;
}